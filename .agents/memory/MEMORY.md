- [Black icon on dark circle](icon-contrast-black-on-dark.md) — solid black glyphs need a light rim stroke to stay visible on dark circle backgrounds; opacity-faded fills look "ashy/washed" instead of true black.
- [Pure black theme overhaul](pure-black-theme.md) — dark theme uses #000000 background; all nav/quick-action icons migrated from webp PackIcon to TpIcon SVG strokes; webp illustrations repurposed as decorative design elements.
- [Metro web port-check workaround](metro-web-static-serve.md) — expo start --web never passes Replit's port-5000 HTTP-200 check (times out even at 180s); fix: expo export --platform web then serve dist/ with scripts/web-serve.js (zero-dep Node http server); rebuild dist after each code change with the export command.
- [Linked account context pattern](linked-accounts-context.md) — active account balance/accountNumber must come from linkedAccounts[activeAccountId], not from user; all screens must pull from the active account, with TrustPoint user data as the fallback.
- [Bank logo SVG assets](bank-logo-svgs.md) — real brand SVGs live in assets/banks/ (extracted from attached_assets/bank_logos_*.zip); inlined as string constants in constants/bank-svgs.ts; rendered via SvgXml from react-native-svg; wordmarks get white pill container auto-sized by aspect ratio.
- [Artifact expo URL fix](artifact-expo-url.md) — the .expo. subdomain routes to the artifact-managed workflow which runs the dev script; set dev script to "node scripts/web-serve.js" so the artifact URL serves the static export instead of the broken Expo dev server.
